              || *) Finish to add the comments inside the functions [[[PARTIAL!]]];
              || 
              || *) Finish the README.md file [[[PARTIAL!]]];
DOCUMENTATION || 
STAGE         || *) Finish the User_Guide.pdf file [[[PARTIAL!]]];
              || 
              || 12.1) Define which license will be used in this project;
              || 
              || 12.2) Add any license note when calling solve_lp or in any part of the help?
              ||
              || ~EXTRA~) Add type hints to the functions. [[[PARTIAL!]]]

*** Finally: Change the repository to public instead of private. ***

-> Next Stage Ideas:
    -> Add the linear solution procedure to be used (primal, dual, ...);
    -> Add the "autofill" to empty values in the matrix input;
