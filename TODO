              || *) Finish to add the comments inside the functions;
              || 
              || *) Finish the README.md file;
DOCUMENTATION || 
STAGE         || *) Finish the User_Guide.pdf file;
              || 
              || ~EXTRA~) Add type hints to the functions.

-> Next Stage Ideas:
    -> Add the linear solution procedure to be used (primal, dual, ...);
    -> Add an "autofill" to empty values in the matrix input?
